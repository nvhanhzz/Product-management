extends ../../layouts/default.pug
include ../../mixins/box-head.pug
include ../../mixins/pagination.pug
include ../../mixins/productLayout.pug

block main
    .container.my-3 
        .row   
            .col-12 
                +box-head(`Result for '${pageTitle}'`)

        +gridLayout(products)
    .pagination
        +pagination(pagination)