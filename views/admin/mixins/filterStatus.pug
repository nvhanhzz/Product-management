mixin filter-status(filterStatus)
    each item in filterStatus   
        button(
            class=`btn-active btn btn-sm ml-1 btn-outline-success ${item.class}`
            status=item.status 
        ) #{item.name}